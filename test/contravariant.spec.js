import { demo} from '../src/contravariant';
const expect = require('chai').expect;
xdescribe('contravariant', () => {
  it('When form the first obtain a objet for first, previous', () => {
    demo();
  });
});